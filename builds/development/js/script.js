$(document).ready(function () {

		Detectizr.detect({detectScreen:false});

		// scroll animation
		/*
		var waypoint = new Waypoint({
			element: $('article .second-block .row div.girl-left'),
			handler: function() {
				$(this.element).animate({
					left: '-8'
				});
			},
			offset: '50%'
		});
	*/
});